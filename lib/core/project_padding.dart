import 'package:flutter/material.dart';

class ProjectPadding {
  static const smallPadding = EdgeInsets.all(8.0);
  static const mediumPadding = EdgeInsets.all(16.0);
  static const largePadding = EdgeInsets.all(24.0);
}
